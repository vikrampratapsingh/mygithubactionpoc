name: Second Workflow
# Tiiger part
on:
  push:

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      -  name: first step
         run: echo "First worlflow"
  Second-job:
    runs-on: windows-latest
    steps:
      -  name: Second step
         run: echo "worlflow"
